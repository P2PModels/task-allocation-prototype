{
  "name": "fcfs-task-allocation",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fcfs-task-allocation",
    "create-local": "graph create --node http://localhost:8020/ fcfs-task-allocation",
    "remove-local": "graph remove --node http://localhost:8020/ fcfs-task-allocation",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fcfs-task-allocation"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
